# ExamHub - AI Prompt Template สำหรับสร้างข้อสอบ

คุณสามารถคัดลอกข้อความคำสั่ง (Prompt) ในกรอบด้านล่างนี้ ส่งต่อไปยัง AI (เช่น Gemini, ChatGPT, Claude) เพื่อให้ AI เจนข้อสอบเวอร์ชันที่ถูกต้องนำเข้ามาในแอปพลิเคชัน **ExamHub** ได้ทันที

---

## 📋 คำสั่งสำหรับส่งให้ AI (Copy and Paste to AI)

```text
กรุณาสร้างชุดข้อสอบเรื่อง [พิมพ์หัวข้อวิชาที่ต้องการตรงนี้ เช่น: ความรู้เกี่ยวกับ Docker และ Kubernetes] 
จำนวน [พิมพ์จำนวนข้อที่ต้องการตรงนี้ เช่น: 5] ข้อ 
โดยส่งผลลัพธ์กลับมาในรูปแบบไฟล์ JSON โครงสร้างตามตัวอย่างที่กำหนดให้อย่างเคร่งครัด โดยห้ามมีอักษรหรือคำอธิบายอื่นนอกเหนือจากบล็อกโค้ด JSON

เงื่อนไขในการสร้าง:
1. ให้มีทั้งข้อที่เป็น "single-choice" (ตอบถูกข้อเดียว) และ "multi-choice" (ตอบถูกหลายข้อ) ปะปนกัน
2. ในส่วนของโจทย์ (text) หากโจทย์เป็นภาษาอังกฤษ ให้เขียนคำแปลภาษาไทยแนบไว้ข้างใต้โจทย์โดยใช้ฟอร์แมต '\n(แปลไทย: ...)' 
3. ดัชนีเฉลย (correctAnswer) อ้างอิงตามลำดับช้อยส์ เริ่มต้นที่ 0 (A=0, B=1, C=2, D=3, E=4, F=5, G=6)
   - หากเป็นโจทย์ single-choice ให้ correctAnswer เป็นตัวเลขเดี่ยว เช่น 2
   - หากเป็นโจทย์ multi-choice ให้ correctAnswer เป็นอาเรย์ของตัวเลขที่เรียงจากน้อยไปมาก เช่น [0, 2]
4. อธิบายเหตุผลเฉลยในฟิลด์ explanation เป็นภาษาไทยอย่างละเอียด

นี่คือโครงสร้าง JSON ตัวอย่างที่ต้องการให้คุณทำตาม:
{
  "title": "ชื่อหัวข้อข้อสอบ",
  "description": "คำอธิบายวิชาสั้นๆ",
  "timeLimit": 15,
  "passPercentage": 70,
  "questions": [
    {
      "text": "What is the default port for HTTP traffic?\n(แปลไทย: พอร์ตมาตรฐานสำหรับทราฟฟิก HTTP คือพอร์ตใด?)",
      "type": "single-choice",
      "options": ["Port 21", "Port 22", "Port 80", "Port 443"],
      "correctAnswer": 2,
      "explanation": "พอร์ต 80 เป็นพอร์ตมาตรฐานสำหรับโปรโตคอล HTTP ส่วนพอร์ต 443 ใช้สำหรับ HTTPS"
    },
    {
      "text": "Which of the following are private IP address ranges? (Choose two)\n(แปลไทย: ข้อใดต่อไปนี้คือช่วงของหมายเลข Private IP Address? (เลือก 2 คำตอบ))",
      "type": "multi-choice",
      "options": [
        "10.0.0.0 to 10.255.255.255",
        "169.254.0.0 to 169.254.255.255",
        "172.16.0.0 to 172.31.255.255",
        "192.168.0.0 to 192.168.255.255",
        "224.0.0.0 to 239.255.255.255"
      ],
      "correctAnswer": [0, 2, 3],
      "explanation": "ช่วงของ Private IP ตามมาตรฐาน RFC 1918 ได้แก่ คลาส A คือ 10.0.0.0/8, คลาส B คือ 172.16.0.0/12 และคลาส C คือ 192.168.0.0/16"
    }
  ]
}
```

---

## ⚙️ ข้อจำกัดของระบบที่ควรรู้ (System Limits)

1. **ประเภทโจทย์:** รองรับ `single-choice` (ตอบถูกข้อเดียว) และ `multi-choice` (ตอบถูกหลายข้อ)
2. **จำนวนตัวเลือก (ช้อยส์):** รองรับได้ตั้งแต่ **2 ถึง 7 ตัวเลือก** ต่อคำถาม
3. **การดึงคำแปลอัตโนมัติ:** เมื่อพิมพ์โจทย์ขึ้นต้นด้วยโจทย์หลัก และขึ้นบรรทัดใหม่ตามด้วยรูปแบบ `\n(แปลไทย: ...)` ระบบจะนำข้อความภาษาไทยส่วนนี้ไปจัดรูปแบบเป็นตัวอักษรสีเทา ขนาดเล็กเอียง อยู่ใต้โจทย์หลักให้อัตโนมัติในทุก ๆ หน้าจอ
4. **ความจุข้อสอบรวม:** ระบบใช้ `LocalStorage` ของเบราว์เซอร์ ซึ่งเก็บข้อมูลได้สูงสุดราว ๆ 5MB (สามารถเก็บข้อสอบเฉลี่ยได้รวมกันประมาณ **2,000 - 5,000 ข้อ**) แต่อย่างไรก็ตาม แนะนำว่าหนึ่งชุดข้อสอบควรมีจำนวนคำถามไม่เกิน **100-200 ข้อ** เพื่อความเร็วสูงสุดในการสลับหน้าจอของเบราว์เซอร์
